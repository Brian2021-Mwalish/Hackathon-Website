# TODO: Move Django Apps to bitsa-backend Folder

- [ ] Move `accounts/` to `bitsa-backend/`
- [ ] Move `authapp/` to `bitsa-backend/`
- [ ] Move `myuserapp/` to `bitsa-backend/`
- [ ] Move `user_app/` to `bitsa-backend/`
- [ ] Move `users/` to `bitsa-backend/`
- [ ] Delete root `authentication/` directory
- [ ] Verify all apps are moved correctly
- [ ] Run `python manage.py check` in `bitsa-backend/` to ensure Django can find the apps
